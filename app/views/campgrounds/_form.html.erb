<%= form_with(model: campground) do |form| %>
  <% if campground.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(campground.errors.count, "error") %> prohibited this campground from being saved:</h2>

      <ul>
        <% campground.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :price_range, style: "display: block" %>
    <%= form.text_area :price_range %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
